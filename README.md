# Nix CoreBlow Tools

Nix tools for CoreBlow.

This repository packages small CoreBlow operator tools through Nix. It is split
from `nix-coreblow` so package metadata and utility tooling can evolve on their
own cadence.

## Development

```sh
nix flake check
```
